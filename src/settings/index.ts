export type { PluginSettings, ViewSettings } from "./types";
export {
	DEFAULT_SETTINGS,
	MAX_LINK_DEPTH,
	MIN_LINK_DEPTH,
	clampLinkDepth,
} from "./defaults";
export { normalizePluginSettings } from "./normalize";
